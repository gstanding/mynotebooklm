# MyNotebookLM

**Your Personal AI Knowledge Assistant**  
**你的专属 AI 知识库助手**

MyNotebookLM 是一个轻量级、本地化部署的 RAG（检索增强生成）系统。它允许你创建多个独立的知识空间（Notebooks），上传 PDF、文本或网页链接，然后通过 AI 进行基于事实的问答。

支持 **DeepSeek V3** 等兼容 OpenAI 接口的模型。

---

## ✨ 核心特性 (Core Features)

### 📚 空间隔离 (Notebooks)
- 创建无限个独立的知识库（如“Python学习”、“旅游攻略”）。
- 每个 Notebook 的数据和索引物理隔离，互不干扰。
- 支持一键切换上下文。

### 🧠 强大的数据摄取 (Ingestion)
- **PDF 解析**：支持提取文本，且内置 **OCR 引擎** (Tesseract + OpenCV)，能自动识别扫描件和插图中的文字。
- **网页抓取**：智能降级策略（Requests -> Pyppeteer -> Raw HTML），支持解析动态网页和单页应用 (SPA)。
- **格式支持**：支持 `.txt`, `.md`, `.pdf` 及 URL。

### 🔍 精准检索与回答 (RAG)
- **混合检索算法**：结合 BM25（关键词匹配）、Jaccard（短语重叠）和 Trigram（模糊匹配），不依赖重型向量库也能实现高召回。
- **可信引用**：AI 的每一句回答都会标注来源（如 `[1]`），点击即可追溯原文。
- **数学公式渲染**：内置 MathJax 引擎，完美渲染 LaTeX 格式的数学公式（如 $E=mc^2$）。

### 🛡️ 数据安全与隐私
- **本地优先**：所有源文件存储在本地磁盘 (`data/` 目录)，不上传第三方云端。
- **资料管理**：支持随时**禁用/启用**特定资料，灵活控制 AI 的知识范围。

---

## 🛠️ 技术栈 (Tech Stack)

- **Backend**: Python 3.6+, FastAPI, Uvicorn
- **Frontend**: HTML5, Vanilla JS, MathJax
- **OCR**: Tesseract 4.0+, OpenCV (图像预处理), PyMuPDF (PDF 渲染)
- **NLP**: Jieba (中文分词), Rank-BM25
- **Browser**: Pyppeteer (基于 Chromium 的无头浏览器)

---

## 🚀 快速开始 (Quick Start)

### 1. 环境准备
确保你的系统已安装 Python 3.6+ 和 Tesseract OCR 引擎。

**macOS (Homebrew):**
```bash
brew install tesseract tesseract-lang
```

**Ubuntu/Debian:**
```bash
sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim
```

### 2. 安装依赖
```bash
pip install -r requirements.txt
# 注意：如果安装 opencv-python 失败，请尝试指定旧版本：
# pip install opencv-python==4.3.0.38
```

### 3. 配置 API Key
设置 DeepSeek 或 OpenAI 的 API Key：
```bash
export DEEPSEEK_API_KEY="your-api-key-here"
```

### 4. 启动服务
```bash
uvicorn app.main:app --reload
```
访问 http://localhost:8000 即可使用。

---

## 📂 项目结构

```
mynotebooklm/
├── app/
│   ├── main.py          # FastAPI 入口
│   ├── ingest.py        # 数据摄取 (PDF/URL/OCR)
│   ├── ocr.py           # OCR 封装 (Tesseract + OpenCV)
│   ├── retrieval.py     # 混合检索逻辑
│   ├── chat.py          # LLM 交互
│   └── static/          # 前端资源
├── data/                # 数据存储 (自动生成)
│   ├── notebooks.json   # Notebook 元数据
│   └── notebooks/       # 各 Notebook 的具体数据
├── docs/                # 项目文档
│   ├── 功能设计文档.md
│   ├── 技术演进路线图.md
│   └── 版本更新说明.md
└── requirements.txt
```

## 📝 版本历史
查看 [docs/版本更新说明.md](docs/版本更新说明.md) 获取详细的更新日志。

---

## 📄 License
MIT License
